version: "3"
services:
  test:
    build: .
    volumes:
      - $PWD:/working
    container_name: sigir2021
    working_dir: /working
    ports:
      - 5000:5000
    environment:
      - PYTHONPATH=/working
    tty: true
